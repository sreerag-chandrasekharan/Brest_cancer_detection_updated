
stages:
  train:
    cmd: python src/train.py
    deps:
      - src/train.py
      - data/raw
      - params.yaml
    outs:
      - models/log_model.pkl
      - models/mlflow_model
    metrics:
      - metrics.json:
          cache: false